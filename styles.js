import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  waiting: {
    color: "#0f0f0f",
    backgroundColor: "#ccc"
  },
  done: {
    color: "#f0f0f0",
    backgroundColor: "#008080"
  }
});
